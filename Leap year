#include<stdio.h>
int main()
{
    int year;
    printf("enter any year : ");
    scanf("%d",&year);
    if( year%4 || year%100 || year%400 == 0){
        printf("leap year");
    }
    else{
        printf("not a leap year");
    }
    // sap id 500107150
}
